"""calc — a tiny integer expression evaluator.

Stub — not implemented yet. The tests and the CLI both expect a top-level
`evaluate(str) -> int`.
"""


def evaluate(expr: str) -> int:
    raise NotImplementedError(
        "not built yet — see the tickets under .scratch/ (run `tracker ready`)"
    )
